#include "Window.h"

//Window::Window(const int width = 640, const int height = 480, const char* title = "Main")
//    : Window::window(glfwCreateWindow(width, height, title, nullptr, nullptr)) {
//  if (!Window::window) {
//    std::cerr << "Cannot create GLFW window." << std::endl;
//    std::exit(EXIT_FAILURE);
//  }
//
//  glfwMakeContextCurrent(Window::window);
//  gladLoadGL();
//  glfwSwapInterval(1);
//
//  glfwSetWindowSizeCallback(Window::window, resize);
//
}